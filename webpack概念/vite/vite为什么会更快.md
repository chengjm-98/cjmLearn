# vite 为什么会更快

## 按需编译

- 不进行全量打包，而是利用浏览器原生支持的 ESM 模块加载机制，在开发阶段按需编译。
- Vite 会使用 ESM（即 <script type="module">）让浏览器自己去请求所需要的模块，并仅在请求时编译文件。首次访问文件时会编译，之后会缓存编译结果。

## 依赖预构建

- Vite 在启动时对 node_modules 中的依赖做一次 预构建（基于 esbuild），并将其缓存起来。此后，浏览器直接请求这些模块时，Vite 直接返回缓存的版本，避免了重复处理。

## 原生浏览器支持 ESM

## 热更新颗粒度更细
