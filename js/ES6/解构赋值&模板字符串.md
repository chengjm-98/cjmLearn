# 解构赋值

const { a, b } = obj;
const [x, y] = arr;

# 展开运算符

const arr2 = [...arr1];
const obj2 = { ...obj1 };

- 合并对象
  const obj3 = {...obj1, ...obj2 };
- 合并数组
  const arr3 = [...arr1,...arr2 ];
- 浅拷贝
