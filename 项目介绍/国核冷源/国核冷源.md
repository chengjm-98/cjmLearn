基于 mapbox 和 three.js 的前端可视化渲染，包括地图渲染，大图打点，云图渲染播放，模型加载等功能，不仅是对当前时段的展示还有未来时段的预测，同时还配合左右两侧的一些数据的可视化展示和一些后台管理部分。同时有的项目还会有 h5 的简易精简版本。

地图渲染部分基于公司内部自研的地图相关的引擎和插件，基于 mapbox 的 webgl 的封装和二次开发，前端调用它的 api 进行开发。

- 遇到的问题
  - 多帧数云图的渲染和播放浏览器内存占有到顶的问题。
  - 大文件，比如一些水利和海洋政策文件报告的流式分片渲染。
